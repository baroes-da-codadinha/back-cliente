const knex = require('../conexao');

const cadastrarEndereco = async (req, res) => {
    const {  } = req.body;

    try {
        
    } catch (error) {
        
    }
}

module.exports = cadastrarEndereco;